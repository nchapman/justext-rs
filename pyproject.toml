[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "justext-uniffi"
version = "0.2.0"
requires-python = ">=3.8"

[tool.maturin]
manifest-path = "uniffi/Cargo.toml"
bindings = "uniffi"
